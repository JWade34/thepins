new stuff

<div class="container">
<%= simple_form_for @pin do |f| %>
  <%= f.input :photo, hint: "Please paste a url with the HTTP", label: "Photo URL" %>
  <br>
  <%= f.input :url, label: "Link to website (URL)" %>
  <br>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
</div>
